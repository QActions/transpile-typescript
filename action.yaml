name: "Transpile typescript"
description: "Builds the dependecies"
runs:
  using: composite
  steps:
    - name: Transpile typescript
      run: |
        yarn
        yarn build
        yarn --production
      shell: bash
